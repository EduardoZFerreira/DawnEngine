package com.boss.world;

import java.awt.image.BufferedImage;

public class FloorTile extends Tile {
    public FloorTile(Integer x, Integer y, BufferedImage sprite) {
        super(x, y, sprite);
    }
}
